# ===========================================
# Variáveis de Ambiente para Azure App Service
# ===========================================

# Porta (Azure define automaticamente)
PORT=8080

# Ambiente
NODE_ENV=production

# Azure SQL Database
DB_HOST=seu-servidor.database.windows.net
DB_USER=seu_admin
DB_PASSWORD=SuaSenhaForte123!
DB_NAME=cloudpartner_hub
DB_PORT=1433
