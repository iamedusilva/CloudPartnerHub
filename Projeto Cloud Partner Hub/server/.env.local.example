# Teste Local com Azure SQL Database
# Configure o .env com suas credenciais do Azure SQL Database antes de executar

# Exemplo de .env para desenvolvimento local:
PORT=3001
NODE_ENV=development

# Azure SQL Database (exemplo)
DB_HOST=seu-servidor.database.windows.net
DB_USER=seu_admin
DB_PASSWORD=SuaSenha123!
DB_NAME=cloudpartner_hub
DB_PORT=1433
